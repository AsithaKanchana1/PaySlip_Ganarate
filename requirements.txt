# New Lanka Clothing — Pay Slip Generator (CLI / Python Script version)
# Install with:
#   pip install -r requirements.txt

openpyxl>=3.1.0    # Read Excel (.xlsx) salary data
reportlab>=4.0.0   # Generate PDF pay slips
